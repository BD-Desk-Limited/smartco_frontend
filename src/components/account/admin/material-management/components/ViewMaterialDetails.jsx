import React from 'react'

const ViewMaterialDetails = ({materialData}) => {
  return (
    <div>Viewing details of material with Id: {materialData.id}</div>
  )
}

export default ViewMaterialDetails;